#!/usr/bin/env node

var busterServer = Object.create(require("./../lib/buster-server"));
var http = require("http");

http.createServer(function (req, res) {
    if (busterServer.respond(req, res)) return;

    res.writeHead(404);
    res.end();
}).listen(4224);